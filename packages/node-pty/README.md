# AgentOS node-pty

`@rivet-dev/agentos-node-pty` implements the `@lydell/node-pty` JavaScript API
on AgentOS kernel PTYs. It is intended for packages running inside an AgentOS
VM and does not load a native Node addon.
